# Vor erster Ausführung muss in dem Configuration enum der "pathToJarsignerDirectory"-String so angepasst werden, dass er auf die jeweilige jarsigner.exe des ausführenden Systems verweist !
# Ansonsten funktioniert die App nicht!
