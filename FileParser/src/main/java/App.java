import java.io.IOException;


public class App {
/*

    public static void main(String... args) throws IOException {

        JSON_parser test = new JSON_parser();

        String path = "gin_data/gin.json";

        Gin[] gin = test.parseJson(path);

        System.out.println(gin.length);

        CSV_parser testCSV = new CSV_parser();

        String pathCSV = "gin_data/gin.csv";

        Gin[] ginCSV = testCSV.parseCSV(pathCSV);

        System.out.println(ginCSV.length);

        XML_parser testXML = new XML_parser();

        String pathXML = "gin_data/gin.xml";

        Gin[] ginXML = testXML.parseXml(pathXML);

        System.out.println(ginXML.length);

    }
*/

}
